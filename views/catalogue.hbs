<div class="container shop-section text-center" id="shop-section"></div>
  <h4>{{title}}</h4>
  <!-- Search Bar -->
  <div class="shop-search">
    <form>
      <table class="table table-borderless">
        <tr>
          <td>
            <input class="form-control" type="search" placeholder="Search">
          </td>
          <td class="text-right">
            <button class="btn" id="showSearchBooking" type="submit">Search</button>
          </td>
        </tr>
      </table>
    </form>
  </div>

  <!-- Display Items on Sale -->
  <div class="display-items text-center">
    <div class="row">
      {{#each products}}
      <div class="col-lg-3 col-sm-6">
        <a href={{this.productDetailsLink}}>
          <img src={{this.img}} width="250px" height="250px">
          <p class="features-item-info">{{this.info}}</p>
          <p>{{this.price}}</p>
        </a>
      </div>
      {{/each}}
    </div>

    <div class="row">
      {{#each products}}
      <div class="col-lg-3 col-sm-6">
        <a href={{this.productDetailsLink}}>
          <img src={{this.img}} width="250px" height="250px">
          <p class="features-item-info">{{this.info}}</p>
          <p>{{this.price}}</p>
        </a>
      </div>
      {{/each}}
    </div>

    <div class="row">
      {{#each products}}
      <div class="col-lg-3 col-sm-6">
        <a href={{this.productDetailsLink}}>
          <img src={{this.img}} width="250px" height="250px">
          <p class="features-item-info">{{this.info}}</p>
          <p>{{this.price}}</p>
        </a>
      </div>
      {{/each}}
    </div>

    <div class="row">
      {{#each products}}
      <div class="col-lg-3 col-sm-6">
        <a href={{this.productDetailsLink}}>
          <img src={{this.img}} width="250px" height="250px">
          <p class="features-item-info">{{this.info}}</p>
          <p>{{this.price}}</p>
        </a>
      </div>
      {{/each}}
    </div>

  <!-- Contact Us -->
  <div class="contact-section text-center" id="contact">
    <h4 class="mb-4"> Didn’t quite find what you were looking for or ordering an archived bag in bulk?</h4>
    <a class="btn button" href={{contactUsLink}} role="button">Talk to Us</a>
  </div>

</div>
